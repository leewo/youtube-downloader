<!DOCTYPE html>
<html lang="ko">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>YouTube 다운로더</title>
    <link rel="stylesheet" href="/styles.css">
</head>

<body>
    <div class="container">
        <h1>YouTube 동영상 다운로더</h1>

        <div class="input-section">
            <input type="text" id="videoUrl" placeholder="YouTube URL을 입력하세요" onkeypress="handleKeyPress(event)">
            <button onclick="getVideoInfo()" class="btn">정보 가져오기</button>
        </div>

        <div id="loading" class="loading">
            <div class="spinner"></div>
            <p>영상 정보를 가져오는 중...</p>
        </div>

        <div id="videoInfo" class="video-info">
            <img id="thumbnail" src="" alt="썸네일">
            <h2 id="videoTitle"></h2>
            <p id="videoDuration"></p>

            <div class="download-options">
                <div class="quality-section">
                    <select id="qualitySelect">
                        <option value="">화질 선택</option>
                    </select>
                    <button onclick="downloadVideo()" class="btn primary">비디오 다운로드</button>
                </div>
                <div class="additional-downloads">
                    <button onclick="downloadAudio()" class="btn secondary">MP3 다운로드</button>
                    <button onclick="downloadSubtitle()" class="btn secondary">영어 자막 다운로드</button>
                </div>
            </div>

            <!-- 다운로드 진행 상태 표시 -->
            <div id="downloadProgress" class="progress-container" style="display: none;">
                <div class="progress-label">
                    <span id="downloadType">다운로드 중...</span>
                    <span id="downloadPercent">0%</span>
                </div>
                <div class="progress-bar">
                    <div id="progressFill" class="progress-fill"></div>
                </div>
                <p id="downloadSpeed" class="download-info"></p>
                <p id="downloadSize" class="download-info"></p>
            </div>
        </div>
    </div>

    <script src="/script.js"></script>
</body>

</html>